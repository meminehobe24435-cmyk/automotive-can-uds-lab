[pytest]
; 让 tests/ 里可以直接 `from src.xxx import ...`
pythonpath = .
testpaths = tests
; --strict-markers 保证打错的 marker 会报错，而不是静默通过
addopts = -ra --strict-markers
markers =
    socketcan: 需要真实 Linux SocketCAN(vcan) 接口，没有时自动跳过
